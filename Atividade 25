algoritmo "Verificar triângulo"

var
  real : a, b, c

escreva("Digite o tamanho do segmento de reta A: ")
leia(a)

escreva("Digite o tamanho do segmento de reta B: ")
leia(b)

escreva("Digite o tamanho do segmento de reta C: ")
leia(c)

se (a < b + c) e (b < a + c) e (c < a + b) entao
  escreva("Os segmentos de reta informados formam um triângulo!")
senao
  escreva("Os segmentos de reta informados não formam um triângulo.")
fimse

fimalgoritmo
