algoritmo "Antecessor e sucessor de um número"

var
   inteiro: num, antecessor, sucessor

inicio
   escreva("Digite um número: ")
   leia(num)
   antecessor = num - 1
   sucessor = num + 1
   escreva("O antecessor de " + num + " é " + antecessor + ".")
   escreva("O sucessor de " + num + " é " + sucessor + ".")
fim_algoritmo
