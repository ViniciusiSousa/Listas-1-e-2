Algoritmo "Cálculo de tinta para pintura de parede"
var
   real :   largura, altura, area, litrosTinta
inicio
   escreva("Digite a largura da parede em metros: ")
   leia(largura)

   escreva("Digite a altura da parede em metros: ")
   leia(altura)

   area <- largura * altura
   litrosTinta <- area / 2

   escreva("A área a ser pintada é de ", area, "m²")
   escreva("Será necessário ", litrosTinta, " litro(s) de tinta para pintar a parede")
Fim_Algoritmo
