algoritmo "Média de notas"

var
   nota1, nota2, media: real

inicio
   escreva("Nota 1: ")
   leia(nota1)
   escreva("Nota 2: ")
   leia(nota2)
   media <- (nota1 + nota2) / 2
   escreva("A média entre " + nota1 + " e " + nota2 + " é igual a " + media + ".")
fim_algoritmo
